// Provider code resolves and fetches remote/local entry sources. The Registry
// holds built-in and plugin providers in resolution order; subprocess plugin
// transport itself lives in pkg/plugins.
package services
