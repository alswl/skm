// Package managers holds per-entity business logic: repository operations
// (scan/import/update/lifecycle/convert/discover), the installer
// (links/adapters/install-state), and the provider registry.
package managers
