// Package providers resolves and fetches remote/local entry sources: the
// Provider interface, the Registry that holds them in resolution order, the
// built-in providers (Local, SelfBuild, GitHub, GitLab, Skills.sh), and the
// subprocess plugin protocol.
package providers
