## skm share install

Preview or install an inline share PAYLOAD

```
skm share install PAYLOAD [flags]
```

### Options

```
  -h, --help   help for install
      --yes    install directly without confirmation
```

### Options inherited from parent commands

```
      --config string   config directory holding config.yaml (default ~/.config/skm)
      --dry-run         perform no writes; report intended actions
      --force           explicitly authorize overwrite/destructive actions
      --json            emit a JSON report on stdout
      --root string     repository root (or a skills/ directory); auto-discovered when omitted
      --timing          write timing info to stderr only
```

### SEE ALSO

* [skm share](skm_share.md)	 - Create and install compact skill share PAYLOADs

###### Auto generated by spf13/cobra on 18-Sep-2026
