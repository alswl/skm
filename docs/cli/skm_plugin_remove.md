## skm plugin remove

Remove a plugin from the plugin directory

```
skm plugin remove <name> [flags]
```

### Examples

```
  skm plugin remove ali-skills --kind provider
```

### Options

```
  -h, --help          help for remove
      --kind string   provider or target (needed only when both kinds share the name)
```

### Options inherited from parent commands

```
      --config string   config directory holding config.yaml (default ~/.config/skm)
      --dry-run         perform no writes; report intended actions
      --force           explicitly authorize overwrite/destructive actions
      --json            emit a JSON report on stdout
      --root string     repository root (or a skills/ directory); auto-discovered when omitted
      --timing          write timing info to stderr only
```

### SEE ALSO

* [skm plugin](skm_plugin.md)	 - Manage installed provider/target plugins

###### Auto generated by spf13/cobra on 24-Sep-2026
