## skm deploy

Clone/pull a repository and batch-install selected assets

```
skm deploy [flags]
```

### Examples

```
  skm deploy --repo git@host:team/skills.git --target codex --only review,release
```

### Options

```
  -h, --help             help for deploy
      --only strings     only install these entry names
      --repo string      git URL, local git repo, or plain directory
      --target strings   target name(s); default all
```

### Options inherited from parent commands

```
      --config string   config directory holding targets.json (default ~/.config/skm)
      --dry-run         perform no writes; report intended actions
      --force           explicitly authorize overwrite/destructive actions
      --json            emit a JSON report on stdout
      --root string     repository root (or a skills/ directory); auto-discovered when omitted
      --timing          write timing info to stderr only
```

### SEE ALSO

* [skm](skm.md)	 - Manage a local repository of AI coding skills and commands

###### Auto generated by spf13/cobra on 8-Aug-2026
